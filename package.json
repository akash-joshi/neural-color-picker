{
  "name": "neural-color-picker",
  "version": "1.0.0",
  "description": "NodeJS module using brain.js to guess if the color is light or dark",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Akash Joshi",
  "license": "MIT",
  "dependencies": {
    "brain.js": "^1.1.2"
  }
}
